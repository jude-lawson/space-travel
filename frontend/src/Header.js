import React from 'react'

//Let's make header buttons update the state of the page to make the body call the proper resource from the API

const Header = () => {
  return (
    <NavLink name='spaceports' />
  );
};

export default Header;